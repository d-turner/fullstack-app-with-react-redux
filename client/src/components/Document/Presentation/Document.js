import React from 'react';
import PropTypes from 'prop-types';

export default function Document(props) {
  return (
    <div className="data-list">
      <span>HERE!</span>
    </div>
  );
}

Document.propTypes = {
  //document: PropTypes.objectOf(PropTypes.object).isRequired,
};
