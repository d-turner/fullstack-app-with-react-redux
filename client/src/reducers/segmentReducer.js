const intialState = {
  segments: [
  ],
};

const segmentReducer = function(state = intialState, action) {
  switch (action.type) {
    default:
      return state;
  }
};

export default segmentReducer;
